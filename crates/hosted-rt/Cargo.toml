[package]
name = "hosted-rt"
version = "0.1.0"
edition = "2021"

[lib]
doctest = false

[dependencies]
hosted = { path = "../hosted" }
loader-core = { path = "../loader-core" }

[dev-dependencies]
lmod = { path = "../lmod" }
