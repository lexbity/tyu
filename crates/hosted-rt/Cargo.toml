[package]
name = "hosted-rt"
version = "0.1.0"
edition = "2021"
description = "Host runtime support for panic handling and hosted execution."

[lib]
doctest = false

[dependencies]
hosted = { path = "../hosted" }
