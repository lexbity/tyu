[package]
name = "device-loader-archive"
version = "0.1.0"
edition = "2021"
publish = false

[lib]
crate-type = ["staticlib"]

[dependencies]
loader-core = { path = "../loader-core", features = ["device-loader"] }
