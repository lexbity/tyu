[package]
name = "tooling-tests"
version = "0.1.0"
edition = "2021"

[dependencies]
hosted = { path = "../hosted" }
hosted-rt = { path = "../hosted-rt" }
loader-core = { path = "../loader-core" }
lmod = { path = "../lmod" }
hex = "0.4"

[features]
default = []
encryption = ["hosted/encryption"]

