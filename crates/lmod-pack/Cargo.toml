[package]
name = "lmod-pack"
version = "0.1.0"
edition = "2021"
description = "Pack ELF objects into `.lmod` containers."

[lib]
name = "lmod_pack"
path = "src/lib.rs"

[[bin]]
name = "lmod-pack"
path = "src/main.rs"

[dependencies]
lmod = { path = "../lmod" }
