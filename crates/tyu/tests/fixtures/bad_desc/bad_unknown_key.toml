# E3647: unknown key inside a v2 section is a hard parse error — a typo'd
# key must not silently change the descriptor's identity.
[platform]
name = "bad"
schema = 2
family = "bad"

[[platform.apertures]]
id = 0
name = "a"
kind = "bus"
base = 0x40000000
size = 0x1000
siz = 0x2000