[package]
name = "langc"
version = "0.1.0"
edition = "2021"

[[bin]]
name = "langc"
path = "src/main.rs"
harness = false
test = false
doctest = false

[dependencies]
hosted = { path = "../hosted" }
hosted-rt = { path = "../hosted-rt" }
frontend = { path = "../frontend" }
semantics = { path = "../semantics" }
ir = { path = "../ir" }
codegen-core  = { path = "../codegen-core" }
codegen-x86_64 = { path = "../codegen-x86_64" }
lmod           = { path = "../lmod" }

[dev-dependencies]
