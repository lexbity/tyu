[package]
name = "codegen-core"
version = "0.1.0"
edition = "2021"

[lib]
test = false
doctest = false

[dependencies]
frontend = { path = "../frontend" }
ir = { path = "../ir" }
lmod = { path = "../lmod" }

[dev-dependencies]
ir = { path = "../ir" }
lmod = { path = "../lmod" }
