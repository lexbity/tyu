[package]
name = "codegen-core"
version = "0.1.0"
edition = "2021"
description = "Shared target and backend abstractions for Tyu code generation."

[lib]
doctest = false

[dependencies]
ir = { path = "../ir" }
frontend = { path = "../frontend" }
lmod = { path = "../lmod" }

[dev-dependencies]
ir = { path = "../ir" }
frontend = { path = "../frontend" }
lmod = { path = "../lmod" }
