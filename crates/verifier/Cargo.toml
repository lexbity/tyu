[package]
name = "verifier"
version = "0.1.0"
edition = "2021"
description = "Static verification subsystem: IR op semantics table, obligation model, interval discharge."

[lib]

[dependencies]
ir = { path = "../ir" }

[dev-dependencies]
serde_json = "1"
