[package]
name = "ir"
version = "0.1.0"
edition = "2021"
description = "Intermediate representation and verifier types for Tyu."

[lib]

[dependencies]
frontend = { path = "../frontend" }

[dev-dependencies]
proptest = "1"
