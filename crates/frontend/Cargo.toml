[package]
name = "frontend"
version = "0.1.0"
edition = "2021"
description = "Lexer, parser, spans, and token handling for Tyu source files."

[lib]
doctest = false

[dependencies]

[dev-dependencies]
proptest = "1"
